package com.airline.controller.workingExample;

/*

import com.airline.entity.Passengers;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@CrossOrigin
@RequestMapping(value = "/rest/exw")
public class HPC {
	@Autowired
	private PassengersDaoH passengersDaoH;

	@GetMapping
	@ResponseStatus(HttpStatus.OK)
	public ResponseEntity <List<Passengers>> getAllUser (){
		return new ResponseEntity<> (passengersDaoH.findAll(), HttpStatus.OK);
	}
}
*/
